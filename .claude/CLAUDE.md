# Project Instructions

**Canonical file:** See `../CLAUDE.md` in project root.

This directory (`.claude/`) is for Claude Code internal state only:
- `plans/` — Active plan files
- `memory/` — Auto-memory (session notes)

All project guidance lives in the root `CLAUDE.md` file.
